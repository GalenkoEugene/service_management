<!DOCTYPE html>
<html>
<head>
  <%= favicon_link_tag 'favicon.ico' %>
  <title><%= full_title(yield(:title)) %></title>
  <%= stylesheet_link_tag    'application', media: 'all' %> <!-- , 'data-turbolinks-track' => true -->
  <%= javascript_include_tag 'application' %> <!-- , 'data-turbolinks-track' => true -->
  <%= csrf_meta_tags %>
</head>
<body>
<div id="wrapper">

    	<%= render partial: "welcome/left_menu" unless current_page?(user_session_url)%>
        <%= render partial: "welcome/top_menu" unless current_page?(user_session_url)%>
        <%= render partial: "welcome/top" unless current_page?(user_session_url)%>
<%= yield %>
        <%= render partial: "welcome/bottom" unless current_page?(user_session_url)%>              
</body>
</html>
